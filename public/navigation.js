/* Autogenerated from "/navigation.coffee" - 2013-04-11 23:41:43 +0000 */

(function() {

  $(function() {
    var $compass, $directions, $left, $right, $wheel, index, rotate,
      _this = this;
    $wheel = $(".steering-wheel");
    $compass = $("#spinner");
    $left = $("#left");
    $right = $("#right");
    $directions = $("#directions");
    index = 0;
    $right.on("click", function() {
      index += 1;
      return rotate();
    });
    $left.on("click", function() {
      index -= 1;
      return rotate();
    });
    return rotate = function() {
      $wheel.css({
        transform: "rotate(" + index * 360 + "deg)"
      });
      $compass.css({
        transform: "rotate(" + index * 90 + "deg)"
      });
      if (index === 5) {
        $directions.removeClass().addClass("go-0-instant");
        index = 1;
        rotate();
      } else if (index === -1) {
        $directions.removeClass().addClass("go-4-instant");
        index = 3;
        rotate();
      } else {
        $directions.removeClass().addClass("go-" + index);
      }
      return console.log(index);
    };
  });

}).call(this);
